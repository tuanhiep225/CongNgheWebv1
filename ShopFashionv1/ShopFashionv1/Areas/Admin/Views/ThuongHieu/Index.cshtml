@model List<Model.EF.Hang>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layouts.cshtml";
}
@section header
{
    Quản Lý Thương Hiệu
}
@section jsfooter
{
    <script src="/assets/admin/js/jquery.unobtrusive-ajax.js"></script>
}
<div class="row-fluid sortable">
    <div class="box span8">
        <div class="box-header" data-original-title>
            <h2><i class="halflings-icon white user"></i><span class="break"></span>Quản Lý Thương Hiệu</h2>
            <div class="box-icon">
                <a href="#" class="btn-setting"><i class="halflings-icon white wrench"></i></a>
                <a href="#" class="btn-minimize"><i class="halflings-icon white chevron-up"></i></a>
                <a href="#" class="btn-close"><i class="halflings-icon white remove"></i></a>
            </div>
        </div>
        <div class="box-content">
            <table class="table table-striped table-bordered bootstrap-datatable datatable">
                <thead>
                    <tr>
                        <th>Mã Thương Hiệu</th>
                        <th>Tên Thương Hiệu</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr id="row_@item.MaHang">
                            <td>@item.MaHang</td>
                            <td class="center">@item.TenHang</td>
                            <td class="center">
                                <a class="btn btn-info" href="/Admin/ThuongHieu/Edit/@item.MaHang">
                                    <i class="halflings-icon white edit"></i>
                                </a>
                                @Ajax.ActionLink("Xóa", "Delete", new { @id = item.MaHang }, new AjaxOptions
                           {
                               Confirm = "Bạn có muốn xóa ?",
                               OnComplete = "$('#row_" + item.MaHang + "').remove()",
                               HttpMethod = "Delete"
                           }, new { @class = "btn btn-danger halflings-icon white trash" })
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
            <a class="btn btn-info" href="/Admin/ThuongHieu/Create">

                Thêm Mới

            </a>
        </div>
    </div><!--/span-->

</div>
